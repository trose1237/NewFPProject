@model IEnumerable<NewFP.Models.Tip>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tip1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tip1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipID })
        </td>
    </tr>
}

</table>
